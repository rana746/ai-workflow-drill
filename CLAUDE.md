# Project Rules

- Full Name validation must require at least two words, representing a first and last name.

- Password validation must require at least 8 characters and at least one uppercase letter. The password must not contain the user's full name.

- Form validation must provide clear inline error messages and use appropriate accessibility attributes such as `aria-invalid` and `aria-describedby`.
